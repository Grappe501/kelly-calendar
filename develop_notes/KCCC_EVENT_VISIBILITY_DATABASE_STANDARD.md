# Event Visibility Database Standard

Permanent limited display: calendar name, safe title, general location, start/end. Implemented by `projectSafeEvent` / `getSafeEventForViewer`. Protected personal → BUSY_ONLY.
