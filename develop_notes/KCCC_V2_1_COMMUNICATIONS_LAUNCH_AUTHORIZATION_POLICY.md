# KCCC — Launch authorization policy (D25)

Authorization is separate from readiness. Button label: **Authorize sandbox launch** (never generic “Send”).

Bounds: mode, recipient limit, batch limit, start/end, exact revision + review hashes.

Self-approval is surfaced when preparer == authorizer.
