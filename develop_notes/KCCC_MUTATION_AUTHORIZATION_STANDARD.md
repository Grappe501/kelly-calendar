# Mutation Authorization Standard

Use `authorize(actor, { action, resource })` with named actions from `src/server/auth/actions.ts`.

Default deny. Kelly / Campaign Manager administer. Other access via calendar/team memberships and action rank thresholds.
