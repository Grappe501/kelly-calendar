# Calendar Permission Model

Default deny. Access via system role (Step 4), team bindings, calendar memberships, event overrides, and section permissions. Safe projections omit unauthorized sections server-side.
