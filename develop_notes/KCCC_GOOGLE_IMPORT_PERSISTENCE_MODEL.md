# Google Import Persistence Model

`ExternalCalendarSource` stores fingerprints (not plaintext secret URLs). Import runs/records + `ExternalEventIdentity` unique constraints. Approval transaction requires Step 4 auth.
