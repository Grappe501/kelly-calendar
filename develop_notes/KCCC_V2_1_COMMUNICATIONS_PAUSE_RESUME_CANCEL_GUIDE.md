# KCCC — Pause / resume / cancel (D25)

Pause stops new batches/attempts; submitted provider work may still reconcile.

Resume requires revalidation (authorization, window, kill switches, artifacts).

Cancel stops new work; does not unsend provider-accepted messages.
