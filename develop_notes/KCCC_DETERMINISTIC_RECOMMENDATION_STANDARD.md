# Deterministic Recommendation Standard

Rules before AI. Every recommendation includes reasons, evidence, confidence 0–1, precedence, and `requiresHumanApproval: true`.

Precedence: safety → compliance → candidate → travel → operations → communications → staffing → packing → optimization → historical.

Protected visibility outranks public promotion rules. Rejected recommendations do not immediately reappear.
