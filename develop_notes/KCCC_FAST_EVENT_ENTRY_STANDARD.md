# Fast Event Entry Standard

Prefer taps, dropdowns, chips, checkboxes, and presets. Routes: `/add`, `/add/quick`, `/add/full`, `/add/templates`. Drafts save to H-drive staging and are labeled **DRAFT — NOT YET ON LIVE CALENDAR**.
